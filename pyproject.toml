[project]
name = "reina-firme-strategy-engine"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "duckdb>=1.5.5",
    "mcp>=2.1.1",
    "pandas>=3.0.5",
    "pyarrow>=25.0.1",
    "python-dotenv>=1.2.3",
    "redshift-connector>=2.1.16",
]

[dependency-groups]
dev = [
    "pytest>=8.4.2",
]
